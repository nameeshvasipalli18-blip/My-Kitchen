import './ExcelSplit.css';

const ExcelSplit = () => {
 return (
   <div className="excel-split-container">
     <p className="header">Excel Split</p>
     <div className="split-container">
       <p>Excel import is temporarily out of scope while the authenticated kitchen workflow is being rebuilt.</p>
     </div>
   </div>
 );
};
export default ExcelSplit;